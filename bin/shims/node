#!/usr/bin/env bash
set -euo pipefail

if ! node_path="$(mise which node 2>/dev/null)"; then
  echo "node requires the mise-owned Node baseline; run: just toolchains" >&2
  exit 127
fi

exec "$node_path" "$@"
