# -----------------------------------------------------------------------------
# .gitignore for Dotfiles Repository
# -----------------------------------------------------------------------------

# --- macOS Specific ---
# General macOS system files generated by Finder / Spotlight etc.
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
# Icon previews
Icon?

# --- Secrets / Sensitive Data ---
# Explicitly ignore your secrets file (based on your zsh structure)
secrets.zsh

# Common patterns for secret files
*.secret
secrets.*
*.key
*.pem
credentials.*
config.local.*
misc/.netrc
*.netrc

# Environment variables files (often contain secrets)
.env
.env.*
*.local
# Allow example files to be committed
!*.example
!*.local.example
!secrets.example.*

# Other editor temporary/backup files
*.swp
*~
*.bak
*.tmp

# --- Language / Tool Specific Cache/Logs ---
# Python cache
__pycache__/
*.py[cod] 
.venv/
*.egg-info/
.installed.cfg
*.egg

# Node modules (if you ever run npm/yarn within the dotfiles dir)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Log files
*.log

# --- Build Artifacts / Cache ---
build/
dist/
.cache/


file2md

# --- XDG Auto-generated (if XDG_CONFIG_HOME ever points here) ---
# These are created by apps respecting XDG spec, not intentional config
config/configstore/
config/pnpm/

# --- Other ---
# Add any other specific files or directories you want to ignore below
# For example, if you have large data files or temporary directories:
# large_dataset/
# temp_files/